VITE_API_URL=http://localhost:8787
# Fallback solo para correr el CRM standalone en dev, sin el dashboard.
# En producción NO definir esta var: así la auth queda 100% por JWT del staff.
VITE_API_KEY=fcac8522-f474-4d29-bd4f-92913a816541
# Origen del dashboard (host) que eventualmente puede embeber el CRM. Se valida
# en el handshake postMessage (lib/embed.ts) — no se usa en Fase 1 standalone.
VITE_DASHBOARD_ORIGIN=http://localhost:5175
